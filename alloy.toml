# Test project for the compiler. The examples repository, checked out
# beside this one, holds the sources, so `alloy build` from anywhere in
# this repo compiles all of them into build/.
[build]
in = "../examples"
out = "build"
exclude = []
clean = true
# ship runs on Roblox; check is what luau-lsp sees
artifact = "ship"
