-- A module that ends in `return` sends that value out as its default.
-- The emit appends no export table: the module's own return is the one
-- value the module yields.
local Palette = { dark = "#111111" }

function Palette.tint(hex: string): string
    return hex
end

return Palette
